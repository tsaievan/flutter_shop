import 'package:flutter/material.dart';

class CategorySecond extends StatelessWidget {
  const CategorySecond({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
